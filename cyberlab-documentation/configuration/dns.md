# DNS Configuration

## DNS Resolver
```pfsense
Services -> DNS Resolver
```
1. Enable DNS Resolver

2. DNS Query Forwards: `Enable Forwarding Mode`


```pfsense
System -> General Setup -> DNS Server Settings
```

## Configure DNS Servers

1. Add DNS Server: `1.1.1.1`
